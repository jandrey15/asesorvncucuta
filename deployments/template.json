{
  "variables": {
    "build_number": "{{env `TRAVIS_BUILD_NUMBER`}}"
  },
  "builders": [
    {
      "type": "digitalocean",
      "image": "40142785",
      "region": "nyc1",
      "size": "s-1vcpu-1gb",
      "snapshot_name": "asesorvncucuta-base-{{user `build_number`}}",
      "ssh_username": "root"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": ["cd asesorvncucuta", "git pull origin master"]
    }
  ]
}
